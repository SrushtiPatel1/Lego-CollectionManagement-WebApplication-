<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Set</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            background-image: url('https://i.pinimg.com/originals/08/96/51/0896511df173f914d1ee6bd45bcfac82.jpg');
            background-repeat: repeat;
        }

        .navbar {
            background-color: #a300d9;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
        }

        .navbar a {
            color: #fff;
            text-decoration: none;
            margin: 0 10px;
        }

        .navbar a:hover {
            text-decoration: underline;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #11bae4;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        .hero {
            background-color: #9fec25;
            color: #fff;
            padding: 20px;
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            display: grid;
            gap: 10px;
        }

        .form-group {
            display: grid;
            gap: 5px;
            grid-template-columns: 1fr 1fr;
        }

        label {
            font-weight: bold;
            margin-bottom: 5px;
            grid-column: 1 / span 2;
        }

        input,
        select,
        button {
            width: calc(100% - 10px);
            padding: 10px;
            box-sizing: border-box;
            margin-top: 5px;
            grid-column: span 2;
        }

        button {
            background-color: #fff;
            color: #0074d9;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #eee;
        }

        .btn-error {
        background-color: #ff4136; /* Red color for error */
        color: #fff;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        cursor: pointer;
        margin-top: 10px;
    }

    .btn-error:hover {
        background-color: #d50b00; /* Darker red on hover */
    }
    </style>
</head>
<body>
    <div class="navbar">
        <a href="/">Lego Collection</a>
        <a href="/about">About</a>
    </div>
    <div class="container">
        <div class="hero">
            <h1>Edit Your Imagination</h1>
            <p>Make changes to the set details below:</p>
        </div>
        <form action="/lego/editSet" method="POST">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="<%= set.name %>" required>
            </div>

            <div class="form-group">
                <label for="year">Year:</label>
                <input type="number" id="year" name="year" value="<%= set.year %>" required>
            </div>

            <div class="form-group">
                <label for="num_parts">Number of Parts:</label>
                <input type="number" id="num_parts" name="num_parts" value="<%= set.num_parts %>" required>
            </div>

            <div class="form-group">
                <label for="img_url">Image URL:</label>
                <input type="url" id="img_url" name="img_url" value="<%= set.img_url %>" required>
            </div>

            <div class="form-group">
                <label for="theme_id">Theme:</label>
                <select id="theme_id" name="theme_id" required>
                    <% themes.forEach(theme => { %>
                        <option value="<%= theme.id %>" <%= (set.theme_id == theme.id) ? "selected" : "" %>>
                            <%= theme.name %>
                        </option>
                    <% }) %>
                </select>
            </div>

            <div class="form-group">
                <label for="set_num">Set Number:</label>
                <input type="text" id="set_num" name="set_num" value="<%= set.set_num %>" readonly>
            </div>

            <button type="submit">Update Set</button>

        </form>
        <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-error">Delete Set</a>
    </div>
</body>
</html>
